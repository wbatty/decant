{
  "name": "decant",
  "version": "1.0.1",
  "description": "CLI to clean noisy HTML and convert it to markdown for LLM context.",
  "type": "module",
  "private": true,
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "bin": {
    "decant": "./src/entry.ts"
  },
  "scripts": {
    "clean": "bun run src/entry.ts clean",
    "extract": "bun run src/entry.ts extract",
    "stats": "bun run src/entry.ts stats",
    "test": "bun test",
    "build:binary": "bun build src/entry.ts --compile --outfile dist/decant",
    "package:manifests": "bun run scripts/generate-package-manager-manifests.ts",
    "update-golden": "bun run scripts/update-golden.ts",
    "smoke-check": "bun run scripts/smoke-check.ts",
    "build": "tsc",
    "prepare": "bun run build"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "@opentui/core": "^0.1.79",
    "commander": "^14.0.1",
    "linkedom": "^0.18.12",
    "mammoth": "^1.11.0",
    "turndown": "^7.2.1",
    "turndown-plugin-gfm": "^1.0.2",
    "unpdf": "^1.4.0"
  },
  "devDependencies": {
    "@types/diff": "^8.0.0",
    "bun-types": "^1.3.0"
  }
}
